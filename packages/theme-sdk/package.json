{
  "name": "@volqan/theme-sdk",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "peerDependencies": {
    "@volqan/core": ">=0.1.0"
  },
  "devDependencies": {
    "@volqan/core": "workspace:*",
    "typescript": "^5.9.0"
  }
}
